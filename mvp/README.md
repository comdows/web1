# 플랫폼올 (가칭) — 분야별 플랫폼 디렉토리 (MVP)

사업자·개인이 "나가서 붙을 수 있는 플랫폼"을 **분야별로 정리한 디렉토리**입니다.
수수료·정산 상세 비교가 아니라, **어떤 분야에 어떤 플랫폼이 있는지 + 개략 설명**만 빠르게 훑는 것이 목적입니다.

## 실행
```
# (선택) 분야별 정적 페이지·sitemap 생성
cd mvp && node build.js

# 로컬 서버
python3 -m http.server 8000      # → http://localhost:8000
#  Windows: python -m http.server 8000  /  또는 그냥 index.html 더블클릭
```

## 구성
| 파일 | 역할 |
|---|---|
| `index.html` | 디렉토리 홈 — 검색 + 분야 필터칩 + 분야별 카드(이름·개략설명·지역·공식링크) |
| `data/platforms.js` | 분야(12) + 플랫폼(60) 데이터. 각 플랫폼은 `blurb`(한 줄 소개) |
| `build.js` | 분야별 정적 페이지 `c/{분야}.html`(색인용, JSON-LD ItemList) + sitemap 생성 |
| `assets/style.css`, `assets/app.js` | 스타일 · 공용 로직 |

## 수록 규모: 35개 분야 · 865개 플랫폼
(법률·세무·전문서비스 / 금융·대출·보험 비교 / 렌탈·구독 / 사무·MRO·산업재 B2B 분야 추가,
지자체 공공배달앱·퀵 보강 완료.)

오픈마켓·종합몰 / 홈쇼핑·T커머스 / 자사몰·쇼핑몰 구축 / 소셜·공동구매·특가 / 라이브커머스 /
크라우드펀딩 / 프리랜서·재능마켓 / 배달·주문중개 / 물류·풀필먼트·배송대행 / 수출입·해외판매 /
도매·소싱 / 숙박·공간·투어 예약 / 중고·리커머스 / 콘텐츠·창작 수익화 / 패션·뷰티 버티컬 /
식품·신선·정기배송 / 핸드메이드·작가마켓 / 구인구직·긱워크 / 생활·홈서비스 O2O /
부동산·상업공간 중개 / 뷰티·헬스케어 예약 / 자동차 거래·정비 / 티켓·공연·이벤트 예매 /
반려동물 / 육아·키즈 / 인쇄·굿즈 제작 / 창작물·디지털 자산 유통 / 운동·피트니스·스포츠

> 소규모·신생 플랫폼까지 웹 검색으로 폭넓게 수록했습니다. URL·설명은 검색 기반이라 일부 변동
> 가능성이 있으니, 이상하면 알려주시면 수정합니다. **NEW** 배지 = 최근 출시 신규 플랫폼(`new:true`),
> 상단 "🆕 신규만" 필터로 모아볼 수 있습니다.

## 데이터 품질 점검 도구 (`tools/`)
```
node tools/audit.mjs        # 정적 감사(오프라인): 중복·필드누락·URL형식·약한링크
node tools/checklinks.mjs   # 라이브 링크 상태(200/404) — 인터넷 되는 곳에서 실행
node tools/checklinks.mjs --csv > linkreport.csv
```
- **정적 감사 결과(현재):** 총 1,171개 · 중복 이름/ID 0 · 필드누락 0 · 잘못된 분야/지역 0 · URL형식 오류 0 → **구조적 결함 없음.**
  (참고: 같은 도메인 다중 항목은 쿠팡/카페24 등 동일 운영사의 별개 서비스라 정상. http·앱스토어·punycode 링크는 발견된 실제 URL.)
- **라이브 상태점검은 이 저장소를 만든 샌드박스에선 불가**(외부 접속 차단). 인터넷 되는 곳에서 `checklinks.mjs`로 4xx/5xx/타임아웃만 걸러내면 됩니다(403은 봇차단이라 대체로 정상).

## 원칙
- 각 설명은 **개략 소개(중립·사실)**. 수수료·정산·입점 조건 등 상세는 각 플랫폼 공식 사이트에서 확인.
- 플랫폼 추가·수정은 `data/platforms.js`만 편집하고 `node build.js` 재실행.

> 참고: 이전 버전의 상세 비교·계산기 기능은 방향 전환(디렉토리 중심)에 따라 제거했습니다.
> 필요 시 git 이력에서 복구 가능합니다.
