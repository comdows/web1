/*
 * config.js — 서비스 단계 기능 플래그 (기획서 1→2→3단계 게이트)
 * ─────────────────────────────────────────────────────────────
 * 활성화 방법: 아래 값을 true로 바꾸고 `node build.js` 재실행 후 커밋.
 *   stage2 : 🤝 제휴 매칭 (플랫폼 간 상호송출·교차프로모션 매칭 보드)
 *   stage3 : 🏦 플랫폼 거래소 (M&A·엑시트 매물 리스팅 보드)
 * false인 동안에도 페이지는 존재하며 "준비 중 + 사전등록(수요 신호 수집)"으로 동작한다.
 * URL에 ?preview=1 을 붙이면 플래그와 무관하게 완성 화면을 미리볼 수 있다(데모·검수용).
 *
 * 기획서 준수 사항(§10·§12 v2 판정) — 페이지 문구와 함께 유지할 것:
 *  - 자금 미보유 원칙: 정산·결제는 플랫폼 간 직접. 우리는 매칭·소개만 한다.
 *  - 거래소는 리스팅·연결만: 중개·자문·실사·가치평가를 수행하지 않는다.
 */
(function () {
  var FLAGS = {
    stage2: false,
    stage3: false,
    // 비공개 접수 채널: 운영 이메일을 넣으면 등록·신청 기본 버튼이 "이메일 접수"로 바뀐다.
    // 비워두면 GitHub 이슈 접수만 노출된다(이슈는 공개 게시판 — 페이지가 개인정보 기재 금지를 안내함).
    contactEmail: ""
  };
  if (typeof window !== "undefined") window.SITE_FLAGS = FLAGS;
  if (typeof module !== "undefined" && module.exports) module.exports = FLAGS;
})();
