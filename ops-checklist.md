# 세모플 운영 체크리스트 — 사용자(운영자)만 할 수 있는 작업

> 개발은 끝났고 계정·인증·법적 결정이 필요한 항목들입니다. 위에서부터 레버리지가 큰 순서.
> 각 항목의 상세 절차는 README.md "설정 상태 / 대기 항목"과 연결됩니다.

## 1. 검색엔진 등록 🔴 최우선

프리렌더 페이지 1,637장 + 분야 허브 45장 + EN 641장이 이미 배포돼 있는데, 색인 요청을 한 번도
안 해서 검색 유입의 첫 단추가 안 끼워진 상태입니다. 등록만 하면 자산이 일하기 시작합니다.

1. **Google Search Console** — https://search.google.com/search-console
   - 속성 추가 → "URL 접두어" → `https://comdows.github.io/web1/`
   - 인증 방식: **HTML 파일** 선택 → 받은 `google*.html` 파일을 이 세션(또는 다음 세션)에
     전달하면 `app/public/`에 넣고 배포해 드립니다 (직접 하려면: 파일을 `app/public/`에 커밋 → Pages 배포 후 "확인")
   - 인증 후: Sitemaps 메뉴 → `sitemap.xml` 제출
2. **Bing 웹마스터 도구** — https://www.bing.com/webmasters
   - "GSC에서 가져오기"를 지원해 1이 끝나면 클릭 몇 번으로 끝. Bing은 ChatGPT 검색의 소스라 중요.
3. **네이버 서치어드바이저** — https://searchadvisor.naver.com
   - 사이트 등록 → HTML 파일 인증(`naver*.html` — 1과 동일하게 전달 주시면 반영) → 사이트맵 제출
4. 등록 1~2주 뒤부터 GSC "실적" 리포트 확인 — 관리 콘솔 "퍼널·유입" 패널(0017)과 교차 검증.

## 2. 알림 이메일 켜기 (선택 — 리텐션 루프 완성)

코드·DB·수신거부 페이지(`?view=optout`)까지 준비 완료, **스위치만 꺼져 있음**.
절차는 README "알림 이메일 켜기" ①~⑤ 그대로:
Resend 가입 → 발신 도메인 SPF/DKIM/DMARC 인증 → `supabase functions deploy send-notify-email`
+ secrets(RESEND_API_KEY·NOTIFY_EMAIL_FROM·CRON_SECRET) → 처리방침에 알림 메일 항목 반영 →
`app_settings 'notify_email'` enabled:true → 스케줄(Supabase Cron 또는 notify.yml curl 스텝).
제휴 제안 서버 발송(send-proposal)도 같은 인프라를 쓰므로 이때 함께 켜는 것이 효율적입니다.

## 3. 특허 출원 ⏰ 시한 있음

- 대상: 발명 4건 (patent-plan.md — 익명성 자동 검증, 밴드 기반 가치 진단 등 초안 작성돼 있음)
- **시한: 각 발명의 최초 공개일(해당 기능의 머지·배포일)로부터 12개월** — 한국 특허법 §30
  공지예외주장. 유럽·중국까지 갈 거면 원칙적으로 공개 전 출원이라 이미 늦었고, 한·미는 12개월 내 가능.
- 지금 할 일: ① 변리사 선임(선행기술조사 필수 — 초안 그대로 출원 금지, patent-plan.md 경고)
  ② 각 발명의 최초 공개일 증빙(커밋·배포 기록) 정리 ③ 급하면 임시명세서로 출원일부터 선점.
- 중소기업·개인은 출원료·심사청구료 70% 감면 대상.

## 4. 법적 표기 실명 확정

`app/src/legal.tsx`의 사업자 표기가 플레이스홀더입니다. 상호·성명이 확정되면 알려 주세요 —
표기 교체 + TERMS_VERSION 상향을 한 번에 처리해 드립니다. (전자상거래법상 신원 표시는
유료화 전이라도 정확한 것이 안전합니다.)

## 5. 가벼운 운영 루틴 (주 1회면 충분)

- 관리 콘솔 큐 비우기: 제보 검수 · 매물 검수 · 💬 매물 질문 · ⭐ 리뷰 검수 · 운영자 인증 · 소개 대기
  (일일 다이제스트 이슈가 대기 건수를 알려줍니다)
- 자동 수집(월요일 아침) 결과 스팟체크 — 신뢰도 배지가 정확하면 README "자동 등재 켜기" 검토
- 월 1회: 헬스체크 이슈(죽은 링크) 확인, 백업 워크플로 성공 여부 확인

## 6. 유료화 (게이트 도달 시에만 — 아직 아님)

조건: 매물 10건+ 등 이용량 기준(pricing-policy.md). 도달하면 README 항목의 5단계
(통신판매업 신고 → 정책 개정 → 처리방침 증빙 → 30일 공지 → 스위치 온) — 시스템은 준비 완료.

## 새 마이그레이션 규칙 (반복 안내)

새 기능 배포 후 `backend/migrations/00NN_*.sql`이 추가되면 Supabase SQL Editor에서
**해당 파일만** 실행하세요(전부 멱등 — 재실행 안전). ALL.sql은 새 DB 초기화 전용입니다.
현재 대기: **0025_reviews.sql** (이용 후기 — 실행 후 상세 후기 섹션·카드 ★평점·리뷰 검수 큐 동작).
