# 세모플 특허 전략 + 발명 명세서 초안 (v1 · 2026-07)

> ⚠️ **고지** — 이 문서는 변리사에게 전달하기 위한 **발명 설명서(참고 초안)**이며 법률 자문이 아닙니다.
> 실제 출원 명세서·청구범위는 반드시 **변리사**가 선행기술조사 후 작성해야 하고, 청구항 문구 하나로
> 권리 범위가 좌우되므로 이 초안을 그대로 출원하지 마세요.

## 0. 가장 급한 것 — 공개(공지)와 신규성

특허는 **출원일 이전에 공개된 발명**이면 신규성 상실로 거절됩니다. 세모플은 공개 GitHub 리포 +
라이브 서비스라서 지금까지 배포한 모든 기능이 이미 "공지"된 상태입니다. 구제 수단:

| 국가 | 자기 공개 유예 | 비고 |
|---|---|---|
| 한국 | **공개일로부터 12개월** (특허법 §30 공지예외주장 — 출원 시 취지 기재 필요) | 최초 공개일 증빙(커밋·배포 기록) 보관 |
| 미국 | 12개월 grace period | |
| 유럽·중국 | 사실상 없음 | 해외 출원 계획이 있으면 **공개 전 출원**이 원칙 |

**결론: 아래 발명들의 최초 공개일(각 기능의 머지·배포일)로부터 12개월 안에 출원해야 합니다.**
발명 1(익명성 자동 검증)은 이 커밋으로 공개되므로, 이 문서를 들고 **가능한 한 빨리 변리사 상담**을
받으세요. 급하면 **임시명세서 출원**(2020년 도입 — 형식 요건 완화, 출원일 선점)으로 날짜부터 확보할 수 있습니다.

## 1. BM(영업방법) 특허가 성립하는 조건

순수한 영업 아이디어("익명으로 매물을 게시하고 소개한다")는 특허 대상이 아닙니다.
**소프트웨어에 의한 정보처리가 하드웨어(컴퓨터) 상에서 구체적으로 실현**되어야 발명으로 인정됩니다
(특허청 컴퓨터 관련 발명 심사기준). 즉 청구항을 다음 형태로 잡아야 합니다:

- ~를 수행하는 **방법** (서버/단말이 수행하는 단계들)
- ~를 포함하는 **시스템/장치**
- ~를 실행시키는 **컴퓨터 판독 가능 기록매체에 저장된 프로그램**

그래서 아래 발명 3건은 전부 "무엇을 어떤 데이터로 어떻게 판정·전이·통지하는가"라는
**구체적 정보처리 구성**으로 기술했습니다.

## 2. 발명 포트폴리오 (우선순위순)

| # | 발명 | 상태 | 특허성 포인트 |
|---|---|---|---|
| 1 | 디렉토리 사전 기반 익명 게시물 식별정보 누출 자동 검증 | 구현 완료(#40) | 디렉토리 DB를 검증 사전으로 재사용하는 구성 — 기술성 뚜렷, 서비스 핵심 자산과 결합 |
| 2 | 쌍방 동의 상태머신 기반 단계적 연락처 공개(2단 소개) | 구현 완료(#39) | 동의 시각 기록 + 상태 전이 게이트 + 이메일 구조적 비노출 |
| 3 | 익명 리스팅-조건 프로파일 자동 대조 및 인앱 통지 | 구현 완료(#39) | 서버가 개인 식별 없이 클라이언트에서 대조(개인정보 최소화 효과) |
| 4 | 범주형 밴드 기반 비식별 가치 구간 추정 | **이번 커밋으로 구현** | 밴드 입력→밴드 출력·단말 내 연산·미저장 — 규제 정합(비자문·가격 미게시)을 기술 구조로 강제 |

---

## 3. 발명 1 — 명세서 초안

### 발명의 명칭
**플랫폼 디렉토리 데이터베이스를 이용한 익명 거래 게시물의 식별정보 누출 자동 검증 방법 및 시스템**

### 기술분야
온라인 익명 거래 게시판(사업·자산 양수도 리스팅 등)에서 게시물 텍스트에 포함된
작성자 식별 가능 정보를 자동으로 탐지·차단·경고하는 정보처리 기술.

### 배경기술
익명 매물 게시판은 작성자가 무심코 적은 플랫폼명·도메인·정확한 수치로 신원이 유추되어
익명성이 붕괴되는 문제가 있다. 종래에는 운영자의 수기 검수에 전적으로 의존하여
① 검수 지연 ② 검수자 숙련도 편차 ③ 작성 시점의 교정 기회 부재라는 한계가 있었다.
연락처 패턴(전화번호·이메일)의 정규식 차단은 알려져 있으나, **작성자가 속한 도메인의
고유명사 사전과 대조해 신원 유추 위험을 판정**하는 구성은 제공되지 않았다.

### 해결하려는 과제
게시물 접수 시점과 검수 시점 양쪽에서, 식별정보 누출 위험을 위험 등급별로 자동 판정하여
익명성 붕괴를 예방하고 검수 부담을 줄이는 것.

### 과제의 해결 수단 (핵심 구성)
1. **검증 사전의 구성**: 서비스가 보유한 **플랫폼 디렉토리 데이터베이스**(등재 플랫폼명 N건)에서
   일정 길이 이상의 등재명을 추출해 식별정보 검증 사전을 자동 구성한다.
   (사전을 별도 구축하지 않고 서비스의 기존 데이터 자산을 재사용 — 등재가 늘수록 검증력이 함께 증가)
2. **2계층 판정**: 게시물 텍스트를
   - **차단 계층**(연락처·메신저 ID·URL 패턴 — 서버 측 제약조건과 동일 기준의 이중화)과
   - **경고 계층**(ⓐ 도메인 표기 ⓑ 단위 결합 수치[억·만 원·만 명·%] ⓒ 검증 사전 일치)으로 판정한다.
3. **2단 제출 확인**: 차단 계층 검출 시 접수를 거부하고, 경고 계층만 검출되면 위험 근거
   (검출 문자열·사유)를 표시한 뒤 **작성자의 재확인 입력이 있어야** 접수를 완료한다.
   텍스트가 수정되면 재확인 상태를 초기화하여 항상 최신 텍스트 기준으로 판정한다.
4. **검수자 하이라이트**: 관리 화면에서 접수 원문과 게시 초안 양쪽에 대해 동일 판정을 재수행하여
   위험 근거를 하이라이트한다(게시 직전 최종 방어선).
5. **서버 측 제약과의 이중화**: 차단 계층 패턴은 데이터베이스 체크 제약조건과 동일하게 유지되어
   클라이언트 우회 시에도 저장이 거부된다.

### 발명의 효과
- 익명성 붕괴 사고를 접수 시점에 예방(사후 삭제 대비 피해 최소화)
- 디렉토리 성장 = 검증 사전 성장의 선순환
- 검수자 1인 운영에서도 일관된 검수 품질
- 작성자에게 즉시 교정 기회 제공 → 반려율 감소

### 도면의 간단한 설명 (출원 시 흐름도로 작성)
- 도1: 시스템 구성(단말–검증 모듈–디렉토리 DB–검수 화면)
- 도2: 2계층 판정·2단 제출 흐름도
- 도3: 검수 화면 하이라이트 예시

### 실시예
본 리포지토리 `app/src/lib/anonymity.ts`(판정 모듈), `app/src/pages.tsx` SellForm(2단 제출),
`app/src/admin.tsx` DealSubQueue(검수 하이라이트), `backend/migrations/0005_intro.sql`
chk_*_nocontact(서버 이중화)가 구현 실시예에 해당.

### 청구범위 초안 (변리사 보정 전제)
1. **[독립항·방법]** 서버 또는 단말이, (a) 플랫폼 디렉토리 데이터베이스로부터 등재명을 추출하여
   검증 사전을 구성하는 단계; (b) 사용자 게시물 텍스트를 연락처 패턴의 차단 계층과, 도메인 표기·
   단위 결합 수치·상기 검증 사전 일치를 포함하는 경고 계층으로 판정하는 단계; (c) 차단 계층 검출 시
   접수를 거부하고, 경고 계층 검출 시 검출 근거를 표시한 후 사용자의 재확인 입력을 조건으로
   접수하는 단계;를 포함하는 익명 게시물의 식별정보 누출 검증 방법.
2. 제1항에 있어서, 텍스트 수정 시 상기 재확인 상태를 초기화하는 단계를 더 포함하는 방법.
3. 제1항에 있어서, 관리자 검수 화면에서 접수 원문과 게시 초안 각각에 대해 상기 판정을 재수행하여
   검출 근거를 표시하는 단계를 더 포함하는 방법.
4. 제1항에 있어서, 상기 차단 계층의 패턴이 데이터베이스 체크 제약조건과 동일하게 유지되어
   클라이언트 우회 시 저장이 거부되는 방법.
5. **[독립항·시스템]** 상기 방법을 수행하는 검증 모듈·디렉토리 데이터베이스·검수 인터페이스를
   포함하는 익명 거래 게시 시스템.
6. **[독립항·기록매체]** 제1항 내지 제4항 중 어느 한 항의 방법을 컴퓨터에서 실행시키기 위한
   프로그램을 기록한 컴퓨터 판독 가능 기록매체.

---

## 4. 발명 2 — 명세서 초안 (요지)

### 발명의 명칭
**쌍방 동의 이력 기반 단계적 연락처 공개 방법 및 시스템**

### 핵심 구성
1. 매도자·매수자 각각의 **연락처 공유 동의 시각을 데이터 필드로 기록**(매도자: 접수 시,
   매수자: 관심 등록 시 — 동의 없는 레코드는 소개 불가로 판정).
2. 소개 절차를 상태머신으로 관리: `관심 접수 → ①매도자 의사 확인(상대 정보는 익명 요지만 전달)
   → ②소개 발송(양측 이메일 상호 공개 + 후속 절차 안내 포함) → 소개 완료(시각·수행자 기록)`.
3. **게이트 조건**: ②소개 발송 행위가 선행되지 않으면 '소개 완료' 상태 전이가 불가;
   양측 이메일과 동의 이력이 모두 존재해야 ②가 활성화.
4. **구조적 비노출**: 이메일은 관리자 전용 뷰(행 수준 접근 제어)에서만 조회되고
   공개 조회 경로에는 작성자 식별자 자체가 포함되지 않는다.

### 효과
동의 없는 개인정보 제공 원천 차단(개인정보보호법 정합), 소개 이행 시점의 증적 확보
(향후 과금·환불 판정 근거), 일방 통보식 소개로 인한 분쟁 예방.

### 청구항 방향
방법 독립항(동의 시각 기록 → 게이트 판정 → 단계 전이) + 상태머신 종속항 + 뷰 분리 종속항 + 시스템·기록매체 항.

### 실시예
`0004_open.sql`/`0005_intro.sql`(contact_consent_at, introduced_at/by, v_admin_intro_queue),
`admin.tsx` IntroQueue(2단 mailto + 게이트), `pages.tsx` 동의 체크(접수·관심 폼).

---

## 5. 발명 3 — 명세서 초안 (요지)

### 발명의 명칭
**익명 리스팅과 조건 프로파일의 비식별 자동 대조 및 통지 방법**

### 핵심 구성
1. 매수 희망자가 관심 분야·예산 밴드·거래 형태로 구성된 **조건 프로파일(브리프)**을 등록.
2. 익명 리스팅은 코드명·범주·밴드만 담은 **비식별 공개 뷰**로 제공.
3. 대조 연산을 서버가 아닌 **이용자 단말(클라이언트)에서 수행** — 서버는 누가 무엇에
   매칭되는지 알 필요가 없어 개인정보 처리를 최소화(분야 일치 ∧ 형태 호환 판정).
4. 매칭 결과는 이용자 개인 화면(내 활동)에 배너로 통지하고, 관리자 화면에는 브리프별
   매칭 코드명을 표시하여 수동 안내를 보조.

### 효과
알림 인프라 없이 "조건 매칭 안내" 이행, 개인정보 최소 처리(privacy by design), 매칭 로직의
서버 부하 제로.

### 청구항 방향
방법 독립항(비식별 뷰 + 클라이언트 대조 + 개인 화면 통지) + 형태 호환 판정 종속항 + 시스템·기록매체 항.

### 실시예
`lib/api.ts` briefMatchesDeal·v_deals_public, `account.tsx` 매칭 배너, `admin.tsx` IntroQueue 브리프 매칭 표시.

---

## 5-1. 발명 4 — 명세서 초안

### 발명의 명칭
**범주형 밴드 입력 기반 비식별 온라인 사업체 가치 구간 추정 방법 및 시스템**

### 기술분야
온라인 사업체(플랫폼)의 양수도 참고 가치를, 개인·기업 식별 정보와 정확한 재무 수치 없이
범주형 밴드 입력만으로 추정하여 구간(밴드)으로 제시하는 정보처리 기술.

### 배경기술
종래 온라인 사업체 가치 추정 도구(해외 브로커리지의 valuation 계산기 등)는 ① 정확한 매출·이익
수치의 입력을 요구하여 민감 정보가 서버로 전송·축적되고, ② 단일 추정가를 출력하여 이용자가 이를
"평가·자문"으로 신뢰하게 만들며, ③ 추정 결과가 매물 정보와 결합되어 게시되는 구조라서
가격 협상 개입·자문 오인·익명성 붕괴의 원인이 되었다. 특히 중개·자문 자격 규제를 회피해야 하는
정보 게시형 플랫폼에서는 이러한 구조를 그대로 쓸 수 없다.

### 해결하려는 과제
민감 수치의 수집 없이, 그리고 평가·자문으로 오인될 수 있는 단일 추정가의 출력 없이,
매도 희망자가 스스로 기대 가격대를 정렬할 수 있는 참고 정보를 제공하는 것.

### 과제의 해결 수단 (핵심 구성)
1. **밴드 한정 입력**: 연매출 밴드(범주), 이전 자산 구성 집합(체크리스트), 운영 인수인계 기간(범주),
   운영 연차(범주), 분야 그룹만 입력받고 정확한 수치 입력 항목을 두지 않는다.
2. **구간 사상·구간 연산**: 각 밴드를 수치 구간 [하한, 상한]으로 사상하고, 분야 그룹별 기준 배수
   구간에 자산 구성·인수인계·연차 가중치를 결합하되, **하한에는 조정치를 부분 반영**(예: 60%)하여
   출력 구간의 폭이 인위적으로 좁아지지 않도록 유지한다.
3. **밴드 한정 출력**: 결과를 단일 수치가 아닌 구간(예: "약 2억 ~ 6억 원")과 적용 배수 구간으로만
   출력한다 — 단일가 미출력이 자문·감정 오인을 구조적으로 차단.
4. **단말 내 연산·비저장·비결합**: 추정 연산 전체를 이용자 단말(브라우저)에서 수행하고 입력·결과를
   서버로 전송·저장하지 않으며, 매물 게시 데이터 경로와 구조적으로 분리한다(게시물에 가격 정보가
   존재할 수 없음 — 가격 미게시 원칙의 기술적 보장).
5. **가정 전면 공개·신뢰 등급**: 적용된 기준 배수·가중치를 결과와 함께 전부 표시하고, 입력 완전도
   (자산 선택 수·연차 입력 여부)에 따른 신뢰 등급을 병기한다.
6. **(확장 실시예)** 거래소 리스팅의 밴드 분포 통계(비식별)로 분야별 기준 배수 구간을 주기적으로
   보정하는 단계; 매수 희망자용 동일 도구 제공(기대치 양방향 정렬).

### 발명의 효과
- 매도자 기대치 사전 정렬 → 소개 후 협상 결렬율 감소(거래 파이프라인 효율)
- 민감 재무 수치의 미수집(개인정보·영업비밀 최소 처리) — privacy by design
- 단일가 미출력·미저장·게시 분리로 무자격 감정·자문·중개 실질 논란을 기술 구조로 회피
- 참고 가정 공개로 이용자가 결과의 성격(일반 정보)을 오인하지 않음

### 도면의 간단한 설명
- 도1: 밴드 입력 UI–구간 사상–구간 연산–밴드 출력의 데이터 흐름(서버 미경유 표시)
- 도2: 구간 연산 상세(기준 배수 + 가중치, 하한 부분 반영)
- 도3: 결과 화면(구간·배수·신뢰 등급·가정 공개)

### 실시예
본 리포지토리 `app/src/lib/valuation.ts`(구간 사상·구간 연산·신뢰 등급),
`app/src/pages.tsx` ValueCheck(`?view=value-check` — 단말 내 연산, 저장·전송 없음, 게시 경로와 분리).

### 청구범위 초안 (변리사 보정 전제)
1. **[독립항·방법]** 이용자 단말이, (a) 사업체의 연매출 밴드·이전 자산 구성 집합·운영 인수인계
   기간·분야를 포함하는 범주형 입력을 수신하는 단계; (b) 상기 연매출 밴드를 수치 구간으로 사상하고,
   분야별 기준 배수 구간에 상기 자산 구성·인수인계에 대응하는 가중치를 결합하는 구간 연산을
   수행하는 단계; (c) 연산 결과를 단일 수치가 아닌 구간으로만, 적용된 가정과 함께 표시하는 단계;를
   포함하고, 상기 입력과 결과가 서버로 전송·저장되지 않으며 사업체 리스팅 게시 데이터와 결합되지
   않는 것을 특징으로 하는 비식별 가치 구간 추정 방법.
2. 제1항에 있어서, 상기 구간 연산이 구간 하한에 가중치 합의 일부 비율만 반영하여 출력 구간의 폭을
   유지하는 방법.
3. 제1항에 있어서, 입력 완전도에 따라 신뢰 등급을 산출하여 결과와 병기하는 단계를 더 포함하는 방법.
4. 제1항에 있어서, 비식별 리스팅 통계의 밴드 분포로 상기 분야별 기준 배수 구간을 보정하는 단계를
   더 포함하는 방법.
5. **[독립항·시스템]** 상기 방법을 수행하는 단말 측 추정 모듈과, 추정 결과가 결합되지 않는 리스팅
   게시 모듈을 포함하는 온라인 사업체 거래 정보 시스템.
6. **[독립항·기록매체]** 제1항 내지 제4항 중 어느 한 항의 방법을 실행시키기 위한 프로그램을 기록한
   컴퓨터 판독 가능 기록매체.

### 법적 정합 메모 (서비스 원칙과의 관계)
이 기능은 **특정 매물의 평가가 아니라 일반 정보(자가 진단)**이며 ① 무료 ② 밴드 출력
③ 미저장·게시 분리 ④ "감정평가·투자자문 아님" 고지 ⑤ 가정 전면 공개 조건을 유지해야
약관 §1(가치평가 미수행)과 모순되지 않는다. 이 조건들이 곧 청구항의 구성요소이므로,
기능 변경 시(예: 결과 저장, 단일가 출력, 매물 연동) 법적 지위도 함께 달라짐에 유의.

---

## 6. 출원 실행 체크리스트

1. **변리사 선임** — 지역지식재산센터(무료 상담)·특허청 공익변리사 상담부터 가능.
   중소기업·개인은 출원료·심사청구료 감면(70%) 대상.
2. **선행기술조사** — KIPRIS(kipris.or.kr)에서 "익명 게시 검증", "연락처 공개 동의", "매물 매칭"
   등으로 국내 선행 확인 + 해외(Flippa, Acquire.com 등 브로커리지 계열 특허) 검토.
3. **출원 형태 결정** — 정식 명세서가 준비되면 정규 출원, 급하면 임시명세서로 출원일 선점 후
   14개월 내 정규화. 발명 1을 최우선으로, 발명 2·3은 하나의 출원에 병합할지 변리사와 결정.
4. **공지예외주장 준비** — 각 발명의 최초 공개일 증빙: GitHub 커밋·PR 머지 일시, Pages 배포
   기록(Actions 로그), 서비스 스크린샷을 보관. (발명 2·3: PR #39, 발명 1: PR #40,
   발명 4: 본 커밋 — 모두 2026-07-03 머지)
5. **비용 감** — 건당 변리사 수수료 포함 통상 수십~수백만 원대(사무소·범위에 따라 상이),
   출원~등록까지 통상 1~2년(우선심사 신청 시 단축). 정부 IP 지원사업(IP디딤돌·나래 등)으로
   출원 비용 지원 가능.

## 7. 반영 이력
- PR #40: `app/src/lib/anonymity.ts`(발명 1 판정 모듈), SellForm 2단 제출, DealSubQueue 하이라이트,
  본 문서 v1(발명 1~3 + 출원 전략)
- 본 커밋: `app/src/lib/valuation.ts` + `?view=value-check` 가치 자가 진단(발명 4 실시예),
  발명 4 명세서 초안 추가
